export class Ingredient {

  // public name: string;
  // public amount: number;
  // constructor shortcut so that we don't have to declare
  // fields and then assign them in the constructor
  constructor(public name: string, public amount: number) {}
}